local global = ...

--===== shared vars =====--
local loadGame = {
	
}

--===== local vars =====--

--===== local functions =====--

--===== shared functions =====--
function loadGame.start()
	--global.worldGen.generate("flat")
	--global.state = "game"
	--os.execute("echo " .. require("serialization").serialize(global.map) .. " > map.log")
end

function loadGame.update()
	
end

function loadGame.draw()
	
end

return loadGame